apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: random-demo-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx-random-demo-service
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: random-demo-service
            port:
              number: 8686